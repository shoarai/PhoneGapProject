/**
 * © 2014 shoarai
 */

/**
 * 設定値
 */
var config = {
  
  // ビープ音の回数[回]
  BEEP_TIME: 3,
  
  // バイブレートの時間[ms]
  VIBRATE_TIME: 2000,
  
  // 加速度の取得周期[ms]
  ACCELEROMETER_FREAQUENCY: 1000,
  
  // 方位の取得周期[ms]
  HEADING_FREAQUENCY: 1000,
  
  
};
